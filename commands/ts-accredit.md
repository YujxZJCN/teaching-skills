---
description: TS accreditation-mapper `map` mode — outcomes-to-standards matrix
---

Trigger the `accreditation-mapper` skill in `map` mode. Builds the course-outcome → program-outcome → standard-criteria matrix with per-cell evidence status (CLAIMED / EVIDENCED / HOLLOW). The standard's current verbatim text comes from the professor.

Mode reference: `MODE_REGISTRY.md`. Skill entry: `accreditation-mapper/SKILL.md`.
