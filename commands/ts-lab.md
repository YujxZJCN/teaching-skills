---
description: TS lab-forge `lab` mode — complete executable lab package
---

Trigger the `lab-forge` skill in `lab` mode. Builds a complete lab/programming assignment package: handout, starter code that runs as shipped, synthetic dataset with recoverable ground truth, independently solved + executed reference solution, and an autograder. Verification evidence ships with the package.

Mode reference: `MODE_REGISTRY.md`. Skill entry: `lab-forge/SKILL.md`.
