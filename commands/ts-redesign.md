---
description: TS course-designer `redesign` mode — evidence-based course redesign
---

Trigger the `course-designer` skill in `redesign` mode. Runs the alignment audit and six-question diagnostic against the existing course, then re-enters design at the earliest broken phase.

Mode reference: `MODE_REGISTRY.md`. Skill entry: `course-designer/SKILL.md`.
