---
description: TS course-publisher `announcement` mode — student announcement draft
---

Trigger the `course-publisher` skill in `announcement` mode. Action-first announcement draft with every fact traced to the Course Passport; draft only — the professor publishes.

Mode reference: `MODE_REGISTRY.md`. Skill entry: `course-publisher/SKILL.md`.
