---
description: TS ta-coordinator `calibration` mode — grading norming session
---

Trigger the `ta-coordinator` skill in `calibration` mode. Builds a norming session package: anchor set, pre-work, session script, agreement measurement, and the annotated-rubric decisions record.

Mode reference: `MODE_REGISTRY.md`. Skill entry: `ta-coordinator/SKILL.md`.
