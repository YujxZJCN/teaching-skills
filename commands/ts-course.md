---
description: TS course-designer `full` mode — complete backward course design
---

Trigger the `course-designer` skill in `full` mode. Backward design: outcomes → assessment plan → schedule → syllabus, with checkpoints at each phase. Initializes or updates the Course Passport.

Mode reference: `MODE_REGISTRY.md`. Skill entry: `course-designer/SKILL.md`.
