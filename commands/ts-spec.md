---
description: TS submission-auditor `spec` mode — compile a checkable Submission Spec
---

Trigger the `submission-auditor` skill in `spec` mode. Compiles the professor's template / requirements / rubric / exemplar into a Submission Spec: every requirement made checkable, classified deterministic vs judgment, exclusions listed, confirmed at a checkpoint before any audit.

Mode reference: `MODE_REGISTRY.md`. Skill entry: `submission-auditor/SKILL.md`.
