---
description: TS teaching-pipeline `status` mode — where is my course?
---

Trigger the `teaching-pipeline` skill in `status` mode. Reads the Course Passport and calendar position; reports current stage, gate states, pending checkpoints, and next actions.

Mode reference: `MODE_REGISTRY.md`. Skill entry: `teaching-pipeline/SKILL.md`.
