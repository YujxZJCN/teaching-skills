---
description: TS media-scripter `script` mode — mini-lecture video script
---

Trigger the `media-scripter` skill in `script` mode. Spoken-register script with synced visual cues, timing from word count, embedded retrieval prompt, and caption-ready output. Material over ~9 minutes is offered as a segmented series instead.

Mode reference: `MODE_REGISTRY.md`. Skill entry: `media-scripter/SKILL.md`.
